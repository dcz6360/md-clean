"""md-clean: turn web articles into clean Markdown / EPUB.

A small command-line tool that fetches a web page, extracts the main article
content, strips away ads / cookie banners / tracking scripts, and outputs
clean Markdown (optionally also an EPUB for e-book readers).
"""

__version__ = "0.1.0"
